import React,{usecontext} from 'react'
import { AirlineAppContext } from '../context/airline-context';

const LandingPage=()=>{
    const {AirLineData}
    return(
        <div>

        </div>

    );
}