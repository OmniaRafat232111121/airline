import React,{useContext} from 'react'
import { AirlineAppContext } from '../context/airline-context';

const LandingPage:FunctionalX=()=>{
    const {AirLineData}=useContext(AirlineAppContext);
    return(
        <div>

        </div>

    );
}